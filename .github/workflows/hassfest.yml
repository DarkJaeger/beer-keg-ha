name: Validate with hassfest
on:
  push:
    paths:
      - "custom_components/beer_keg_ha/**"
      - ".github/workflows/hassfest.yml"
  pull_request:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: home-assistant/actions/hassfest@master
